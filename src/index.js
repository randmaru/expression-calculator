function eval() {
	// Do not use eval!!!
	return;
}

function expressionCalculator(expr) {
	let openBracket = 0,
		closeBracket = 0,
		string = expr.split("");
}

module.exports = {
	expressionCalculator
}
